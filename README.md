# 🤖 Crypto Trading Bot System

Система для автоматической торговли криптовалютой с бэктестингом, paper trading и веб-дашбордом.

## ✨ Возможности

- 📊 **Загрузка исторических данных** с Binance (все монеты, любые таймфреймы)
- 🧪 **Бэктестинг** стратегий с реалистичными условиями (fees, slippage)
- 📈 **Paper Trading** - торговля на виртуальные деньги в реальном времени
- 🌐 **Веб-дашборд** для мониторинга и управления
- 🔄 **Live Trading** - готово к подключению реальных API ключей

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонирование
cd trading-system

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или: venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Загрузка данных

```bash
# Загрузить данные за 2 года для основных монет
python main.py download --start 2022-01-01

# Или для конкретных монет
python main.py download --symbols BTCUSDT ETHUSDT --intervals 1h 4h 1d --start 2023-01-01
```

### 3. Запуск бэктеста

```bash
# Бэктест стратегии Mean Reversion на BTCUSDT
python main.py backtest --symbol BTCUSDT --interval 1h --strategy mean_reversion --start 2023-01-01

# Доступные стратегии: mean_reversion, trend_following, combined
```

### 4. Запуск дашборда

```bash
python main.py dashboard --port 5000
```

Открыть в браузере: http://localhost:5000

## 🐳 Запуск через Docker

```bash
# Скопировать и настроить .env
cp .env.example .env

# Запустить
docker-compose up -d

# Логи
docker-compose logs -f dashboard
```

## 📁 Структура проекта

```
trading-system/
├── config/           # Конфигурация
│   └── settings.py
├── core/             # Ядро системы
│   ├── binance_client.py    # Binance API
│   └── paper_trading.py     # Paper trading engine
├── data/             # Управление данными
│   └── data_manager.py
├── backtesting/      # Бэктестинг
│   └── engine.py
├── bots/             # Торговые стратегии
│   └── strategies.py
├── dashboard/        # Веб-интерфейс
│   ├── app.py
│   └── templates/
├── main.py           # Точка входа
└── requirements.txt
```

## 📊 Веб-дашборд

### Overview
- Equity curve
- Live цены
- Статистика (win rate, P&L, positions)
- История сделок

### Positions
- Открытие/закрытие позиций вручную
- Stop-loss и Take-profit
- Мониторинг открытых позиций

### Backtest
- Выбор символа, интервала, стратегии
- Визуализация equity curve
- Детальные метрики

### Data
- Загрузка исторических данных
- Статистика по загруженным данным

## ⚙️ Конфигурация

### Trading параметры (`config/settings.py`)

```python
# Risk Management
max_position_pct = 0.02      # 2% на сделку
max_total_exposure = 0.10    # 10% всего капитала
max_daily_loss_pct = 0.05    # 5% макс. дневной убыток

# Stop Loss / Take Profit
default_stop_loss_pct = 0.02   # 2%
default_take_profit_pct = 0.04 # 4%
```

### Binance API

Для paper trading API ключи не нужны. Для реальной торговли:

1. Создать API ключи на https://www.binance.com/en/my/settings/api-management
2. Добавить в `.env`:
```
BINANCE_API_KEY=your_key
BINANCE_API_SECRET=your_secret
```

### Testnet (рекомендуется для тестов)

1. Зарегистрироваться на https://testnet.binance.vision/
2. Создать API ключи
3. Добавить в `.env`:
```
BINANCE_TESTNET_API_KEY=your_testnet_key
BINANCE_TESTNET_API_SECRET=your_testnet_secret
```

## 📈 Стратегии

### Mean Reversion
- Покупка при oversold (Z-score < -2, RSI < 30)
- Продажа при overbought (Z-score > 2, RSI > 70)
- Win rate: ~55-62%

### Trend Following
- MACD crossover + ADX filter
- Следование за трендом
- Win rate: ~48-55%

### Combined
- Комбинация нескольких стратегий
- Сигнал при согласии 2+ стратегий
- Более надёжные входы

## 🔒 Безопасность

- ⚠️ **НИКОГДА** не храните API ключи в коде
- Используйте `.env` файл (добавлен в .gitignore)
- Начинайте с **paper trading**
- Используйте **testnet** для тестов
- Ограничьте права API (только spot trading, no withdrawals)

## 📝 API Endpoints

```
GET  /api/status              # Общий статус
GET  /api/paper/status        # Статус paper trading
GET  /api/paper/positions     # Открытые позиции
GET  /api/paper/trades        # История сделок
POST /api/paper/open          # Открыть позицию
POST /api/paper/close         # Закрыть позицию
POST /api/paper/reset         # Сброс аккаунта
GET  /api/data/available      # Доступные данные
POST /api/data/download       # Загрузить данные
POST /api/backtest/run        # Запустить бэктест
```

## 🎯 Roadmap

- [ ] ML модели (XGBoost, LSTM)
- [ ] Order book analysis
- [ ] Liquidation hunting
- [ ] Multi-symbol portfolio
- [ ] Telegram notifications
- [ ] Live trading mode

## ⚠️ Disclaimer

Эта система предназначена для образовательных целей. Торговля криптовалютой связана с высокими рисками. Используйте на свой страх и риск. Начинайте с paper trading и небольших сумм.

---

Made with ❤️ for crypto traders
